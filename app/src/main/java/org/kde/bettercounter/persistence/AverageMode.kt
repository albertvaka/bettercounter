package org.kde.bettercounter.persistence

enum class AverageMode {
    FIRST_TO_NOW,     // Average between first entry and now
    FIRST_TO_LAST     // Average between first entry and last entry
} 
